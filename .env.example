# Live portfolio (Dhan) — used by `npm run pull:portfolio`.
# Fill with your own values; never commit the real .env.
DHAN_CLIENT_ID=
DHAN_ACCESS_TOKEN=
# Optional override (defaults to https://api.dhan.co)
# DHAN_API_BASE=
